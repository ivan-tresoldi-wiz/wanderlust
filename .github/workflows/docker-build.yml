name: Build Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    name: 'Docker Image Build - Scan - Push'
    runs-on: ubuntu-latest
    env:
      SCAN_PATH: "." # Set the relative path in the repo to build the image from
      IMAGE: "itresoldi/wanderlust"
      IMAGE2: "itresoldi/postgres"
      TAG: "latest" # Set the tag to use for the image
      POLICY_VULN: "ivan-tresoldi-vulnerabilities"
      POLICY_IAC: "ivan-tresoldi-IaC-misconfigurations"
    
    strategy:
      matrix:
        include:
        - image: $IMAGE:$TAG
          dockerfile: ./Dockerfile
        - image: $IMAGE2:$TAG
          dockerfile: database/Dockerfile

    defaults:
      run:
        shell: bash
      
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Download Wiz CLI
      run: curl -o wizcli https://downloads.wiz.io/wizcli/latest/wizcli-linux-amd64 && chmod +x wizcli

    - name: Authenticate to Wiz
      run: ./wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
      env:
        WIZ_CLIENT_ID: ${{ secrets.WIZ_CLIENT_ID }}
        WIZ_CLIENT_SECRET: ${{ secrets.WIZ_CLIENT_SECRET }}

    - name: Run wiz CLI IaC scan
      run: ./wizcli iac scan --path $SCAN_PATH --policy "$POLICY_IAC"
  
    - name: Build Docker IMAGE 1
      run: cd $SCAN_PATH && docker build . --tag $IMAGE:$TAG

    - name: Build Docker IMAGE 2
      run: cd $SCAN_PATH/database && docker build . --tag $IMAGE2:$TAG

    - name: Run wiz-cli docker image scan on IMAGE 1
      run: ./wizcli docker scan --image $IMAGE:$TAG --policy "$POLICY_VULN"

    - name: Run wiz-cli docker image scan on IMAGE 2
      run: ./wizcli docker scan --image $IMAGE2:$TAG --policy "$POLICY_VULN"
    
    - name: Push Docker IMAGE 1
      run: docker push $IMAGE:$TAG
    
    - name: Push Docker IMAGE 2
      run: docker push $IMAGE2:$TAG

    - name: Tag IMAGE 1 for Wiz Image Trust
      run: ./wizcli docker tag --image $IMAGE:$TAG
    
    - name: Tag IMAGE 2 for Wiz Image Trust
      run: ./wizcli docker tag --image $IMAGE2:$TAG